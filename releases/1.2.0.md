## Arches Controlled Lists 1.2.0 Release Notes

### New Features

### Performance Improvements

### Bug fixes

- Normalize whitespace-only lines in `__arches_migrate_collections_to_clm.sql` to stop a phantom `AlterSQL` migration from being generated on every `makemigrations` run under arches 8.2.
- Updated usage of rdflib to cope with 4.2.2 -> 7.6.0 upgrade in Arches 8.2.0 []()

### Dependency changes
```
Python:
    Upgraded:
        arches: >=8.1.0 -> >=8.2.0a1
            rdflib 4.2.2 -> 7.6.0
        arches-component-lab: >=0.0.1a7 -> >=0.0.1a11

    Added:
        None

    Removed:
        None


JavaScript:
    Upgraded:
        arches: dev/8.1.x -> dev/8.2.x

    Added:
        None

    Removed:
        None
```

### Breaking changes

- The minimum supported version of Python is now 3.12.

### Deprecations

### Upgrading Arches Controlled Lists

1. Upgrade python package
    ```
    pip install --upgrade arches-controlled-lists==1.2.0
    ```

1. Update webpack build
    ```
    npm run build_development
    python manage.py collectstatic
    ```

1. If you are running Arches on Apache, restart your server:
    ```
    sudo service apache2 reload
    ```
